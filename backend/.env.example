# Copy to .env and fill in. NEVER commit .env.
SUPABASE_URL=https://dwlzelvxgtpttlfuhhbe.supabase.co
# Supabase dashboard → Project Settings → API → "service_role" secret (NOT the publishable key)
SUPABASE_SERVICE_ROLE_KEY=
# Your OpenRouter key (same value as the app's VITE_OPENROUTER_KEY)
OPENROUTER_KEY=
PORT=8787
